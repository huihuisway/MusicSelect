{
  "name": "musicselect",
  "version": "2.0.0",
  "description": "校园音乐选择工具 - 后端 API + 前端展示",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "client": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "setup": "npm install && cd web && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "lowdb": "^7.0.1",
    "axios": "^1.6.0",
    "node-cache": "^5.1.2",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
